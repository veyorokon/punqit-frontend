import styled from "styled-components";

import {Flex} from "components/core";

const GetImageBox = styled(Flex)`
  background: white;
  box-shadow: 0 27px 24px 0 rgba(0, 0, 0, 0.2);
  border-radius: 2px;
  cursor: default;
  position: relative;
`;
export default GetImageBox;
